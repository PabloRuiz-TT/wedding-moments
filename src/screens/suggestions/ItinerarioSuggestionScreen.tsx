import { Text } from "react-native-paper";
import { SafeAreaView } from "react-native-safe-area-context";

export const ItinerarioSuggestionScreen = () => {
  return (
    <SafeAreaView>
      <Text>Itinerario Suggestion Screen</Text>
    </SafeAreaView>
  );
};
