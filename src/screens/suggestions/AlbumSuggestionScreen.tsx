import React from "react";
import { Text } from "react-native-paper";
import { SafeAreaView } from "react-native-safe-area-context";

export const AlbumSuggestionScreen = () => {
  return (
    <SafeAreaView>
      <Text>Album Suggestion Screen</Text>
    </SafeAreaView>
  );
};
